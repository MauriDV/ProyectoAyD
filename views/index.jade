extends layout

block content
  if (!user)
    a(href="/login") Login
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    p game: #{games}
    a(href="/logout") Logout
    br
    a(href="/createNewGame/#{user.username}")     Create New Game :)
